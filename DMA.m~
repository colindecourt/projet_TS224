function [] = DMA(y)
% Detrending Moving Average (DMA) 

%% Parametres

F=[];
M=length(y);
mu_y=mean(y);

%% Création du profil


for m=1:M
    somme=0;
    for i=1:m
        temp=y(i)-mu_y;
        somme=somme+temp;
    end
    y_init(m)=somme;
    
end

y_filter = filter(b,a,

end

